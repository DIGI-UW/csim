# Live saved-dashboard backup — September 17, 09:30 Pacific

One dashboard, 21 charts and six reporting datasets were exported separately. The capture confirms the objects did not change during export. All object definitions in the dashboard bundle exactly match Beth's attached export; export timestamps differ.

See [capture metadata and ZIP hashes](capture.json), [saved-object inventory](inventory.json) and [handoff report](../../../reports/beth-final-export-and-winter-handoff-2026-09-17.md).

`sourceAssetsRevision: dbb1f00` records the repository checkpoint used for the capture operation. It does not claim Beth's later UI edits were generated by that commit. This is a backup of saved UI state, not an accepted client release. No reporting rows were exported.
